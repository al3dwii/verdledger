{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "apps/**/dist/**",
        "packages/**/dist/**",
        "dist/**",       
        "go/bin/**"
      ]
    },

    
    "test": { "cache": false, "outputs": [] },
    "lint": { "outputs": [] }
  }
}
